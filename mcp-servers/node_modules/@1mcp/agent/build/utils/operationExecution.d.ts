import { OperationOptions } from '../core/types/index.js';
/**
 * Executes an operation with error handling and retry logic
 * @param operation The operation to execute
 * @param contextName The execution context name for logging
 * @param options Operation options including timeout and retry settings
 * @returns The result of the operation
 */
export declare function executeOperation<T>(operation: () => Promise<T>, contextName: string, options?: OperationOptions): Promise<T>;
//# sourceMappingURL=operationExecution.d.ts.map