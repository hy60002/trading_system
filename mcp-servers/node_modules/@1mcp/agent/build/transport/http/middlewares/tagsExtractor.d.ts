import { Request, Response, NextFunction } from 'express';
/**
 * Middleware to extract and validate 'tags' from query parameters.
 * Attaches tags as res.locals.tags (string[] | undefined).
 */
export default function tagsExtractor(req: Request, res: Response, next: NextFunction): void;
//# sourceMappingURL=tagsExtractor.d.ts.map